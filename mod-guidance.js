(function(root,factory){
  const api=factory(root.IterionEngine,root.IterionCircuits,root.IterionData,root.IterionMods);
  if(typeof module==='object'&&module.exports)module.exports=factory(require('./engine.js'),require('./circuits.js'),require('./data.js'),require('./mods.js'));
  root.MonoidModGuidance=api;
})(typeof globalThis!=='undefined'?globalThis:this,function(E,C,D,M){
  'use strict';
  const G={"double-double":{"family":"signal","verb":"USE","build":"Put it on a non-zero Double.","reward":"First activation each Move applies both halves.","market":"Make one Double hit with both halves once per Move.","visual":"double-double","note":"Later passes that Move use the normal Double operation."},"double-echo":{"family":"signal","verb":"USE","build":"Put it on a non-zero Double.","reward":"First activation sends one Echo down the chosen downstream route.","market":"Make one Double replay its downstream route once.","visual":"double-echo","note":"Final Score is Main + Echo. Echo cannot create another Echo."},"zero-port":{"family":"signal","verb":"LINK","build":"Install it on two tiles that contain Zero.","reward":"The signal teleports between them instead of rebounding.","market":"Link two Zero tiles into a teleport.","visual":"zero-port","note":"A receiving [0|0] sends the signal through both ends."},"parity-exchange":{"family":"number","verb":"CHANGE","build":"Put it on any physical tile.","reward":"On this tile, ODD adds and EVEN multiplies.","market":"Swap odd/even scoring on one tile.","visual":"parity","note":"Printed values, matching and routing do not change."},"corner":{"family":"shape","verb":"BUILD","build":"Give this tile exactly 2 neighbours at a right angle.","reward":"Its operation becomes ×3.","market":"Build a right angle around one tile.","visual":"corner","note":"The signal does not need to turn through the tile."},"long-line":{"family":"shape","verb":"BUILD","build":"Give this tile exactly 2 opposite neighbours and extend the same physical line.","reward":"3–4 tiles: ×2. 5+ tiles: ×3.","market":"Build a straight physical line through one tile.","visual":"long-line","note":"Route history does not define the line."},"overload":{"family":"shape","verb":"BUILD","build":"Add physical neighbours around this tile.","reward":"Operation magnitude equals its neighbour count, up to ×4.","market":"Pack neighbours around one tile for up to ×4.","visual":"overload","note":"A complete Double cross reaches ×4."},"terminal":{"family":"shape","verb":"BUILD","build":"Keep this tile with exactly 1 physical neighbour.","reward":"Its operation becomes ×3.","market":"Keep one tile at the edge of the machine.","visual":"terminal","note":"A second neighbour makes it inactive, but the Mod stays installed."},"triple-double":{"family":"signal","verb":"BUILD","build":"Complete all 4 physical sides of a non-zero Double.","reward":"First forward activation clones the signal through the other 3 exits.","market":"Turn a full Double cross into a 3-way distributor.","visual":"triple-double","note":"It can split only once per Move."},"diode":{"family":"signal","verb":"DIRECT","build":"Put it on a non-zero non-double, then choose its IN half.","reward":"Signal passes only when it enters through IN.","market":"Make one tile one-way.","visual":"diode","note":"Reverse entry stops before this tile operates."},"return":{"family":"signal","verb":"RETURN","build":"Give this tile at least 2 usable exits.","reward":"When the first branch dies, reset here and try another exit once.","market":"Turn one dead end into a second route.","visual":"return","note":"The Score resets to the post-RETURN checkpoint; traversed edges stay spent."},"twin":{"family":"shape","verb":"BUILD","build":"Connect this tile directly to another domino with the same printed pair.","reward":"Its operation becomes ×3.","market":"Touch an identical printed domino for ×3.","visual":"twin","note":"The two physical tiles still keep different IDs."},"pair":{"family":"shape","verb":"BUILD","build":"Place an aligned parallel neighbour along this tile’s full long side.","reward":"The exact 2×2 domino block gives ×3.","market":"Make an exact parallel 2×2 block for ×3.","visual":"pair","note":"The two dominoes must align along the full long side."},"bridge":{"family":"shape","verb":"BUILD","build":"Make this tile the only physical link between two parts of the machine.","reward":"Its operation becomes ×3.","market":"Make one tile a structural bridge for ×3.","visual":"bridge","note":"Removing it would split its connected component."},"gate":{"family":"shape","verb":"BUILD","build":"Give a non-double exactly 2 neighbours: one on each half.","reward":"Its operation becomes ×2.","market":"Put one neighbour on each half for ×2.","visual":"gate","note":"The two connections do not need to be in a straight line."},"fan":{"family":"shape","verb":"BUILD","build":"Attach exactly 3 neighbours around the same half; leave the other half open.","reward":"Its operation becomes ×4.","market":"Fan 3 neighbours around one half for ×4.","visual":"fan","note":"All three contacts must use distinct exterior sides."},"frame":{"family":"shape","verb":"BUILD","build":"Make this tile part of at least 1 closed physical loop.","reward":"Its operation becomes ×2.","market":"Put one tile inside a closed loop for ×2.","visual":"frame","note":"Circuit rank is not required."},"crown":{"family":"shape","verb":"BUILD","build":"Give this tile exactly 3 exterior neighbours spanning both halves.","reward":"Its operation becomes ×4.","market":"Build a 3-sided junction across both halves for ×4.","visual":"crown","note":"Unlike FAN, both halves must participate."},"frontier":{"family":"shape","verb":"BUILD","build":"Give this tile 2+ neighbours while keeping one whole long side clear.","reward":"Its operation becomes ×2.","market":"Stay connected but preserve one expansion lane for ×2.","visual":"frontier","note":"The free side must be an entire long side."},"merge":{"family":"signal","verb":"MERGE","build":"Route 2 sibling branches into the same half of this tile.","reward":"Their current Scores add together, then this tile operates once.","market":"Merge 2 signals into 1.","visual":"merge","note":"Triple Double does not partially merge a 3-way split."},"hinge":{"family":"signal","verb":"SWING","build":"Install it where the tile has a valid mirrored position across an adjacent pivot.","reward":"First activation each Move swings it to the stored opposite state.","market":"Make one physical tile move between 2 machine states.","visual":"hinge","note":"The new state persists. It stays put if blocked or if moving would change a Topology Mod."},"resonator":{"family":"state","verb":"DEVELOP","build":"Give this same physical tile Circuit rank.","reward":"Circuit I–II: ×2. Circuit III–V: ×3.","market":"Turn Circuit rank on one tile into operation strength.","visual":"resonator","note":"It does not change Circuit detection or resonance."},"forge":{"family":"state","verb":"DEVELOP","build":"Put it on a tile that already has Stars.","reward":"Star I–II: ×2. Star III: ×3.","market":"Turn existing Stars into operation strength.","visual":"forge","note":"FORGE does not add, consume or move Stars."},"foundation":{"family":"state","verb":"KEEP","build":"Leave this tile in the machine across later Markets.","reward":"After 1 Market: ×2. After 3 Markets: ×3.","market":"Keep one tile across Markets: ×2 → ×3.","visual":"foundation","note":"Reassigning FOUNDATION resets its age."},"knot":{"family":"shape","verb":"BUILD","build":"Make this tile part of 2 or more distinct physical loops.","reward":"Its operation becomes ×4.","market":"Overlap multiple loops through one tile for ×4.","visual":"knot","note":"Route history and Circuit rank do not matter."},"mirror":{"family":"number","verb":"MATCH","build":"Give each half one neighbour, then match the two outward neighbour values.","reward":"Its operation becomes ×3.","market":"Match the two outward neighbour values for ×3.","visual":"mirror","note":"Compare the outer values, not the touching values."},"mint":{"family":"state","verb":"ACTIVATE","build":"Score through this tile.","reward":"The first qualifying activation each round pays +1 coin.","market":"Turn one activation per round into +1 coin.","visual":"mint","note":"MINT does not change Score."},"long-run":{"family":"machine","verb":"ROUTE","build":"Send one Move through at least 10 unique physical tiles.","reward":"Every activated starred tile on that route pays its tier once.","market":"Long routes make every activated Star pay once.","visual":"long-chain","note":"In Endless, the full payout is limited to 7 qualifying Moves."}};
  const escapeHtml=value=>String(value).replace(/[&<>\"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','\"':'&quot;',"'":'&#039;'}[c]));
  const code=id=>M&&M.get(id)&&M.get(id).collectionCode?M.get(id).collectionCode:id.slice(0,2).toUpperCase();
  function get(id){const g=G[id];return g?Object.freeze(Object.assign({id,code:code(id)},g)):null}
  function shapeNodes(pattern){return ({terminal:['w'],corner:['n','e'],'long-line':['ww','w','e','ee'],overload:['n','e','s','w'],twin:['e'],pair:['s'],bridge:['w','e'],gate:['w','e'],fan:['n','e','s'],frame:['n','e','s','w'],crown:['n','e','w'],frontier:['w','e'],knot:['n','e','s','w']}[pattern]||[])}
  function diagramHtml(id,compact){
    const g=get(id);if(!g)return'';const cls=compact?' compact':'';
    if(g.family==='shape'||g.family==='power'){const nodes=shapeNodes(g.visual).map(n=>'<i class="modNode '+n+'"></i>').join('');return '<div class="modDiagram modDiagramShape pattern-'+g.visual+cls+'" role="img" aria-label="'+escapeHtml(g.market)+'"><span class="modCore">'+escapeHtml(g.code)+'</span>'+nodes+'</div>'}
    const map={'double-double':'<b>[DD]</b><i>→</i><b>BOTH HALVES</b>','double-echo':'<b>[DE]</b><i>→</i><b>MAIN + ECHO</b>','zero-port':'<b>[ZP]</b><i>⇢</i><b>[ZP]</b>','parity':'<b>ODD +</b><i>·</i><b>EVEN ×</b>','triple-double':'<b>1 IN</b><i>→</i><b>3 OUT</b>','diode':'<b>IN</b><i>→</i><b>OUT</b>','return':'<b>DEAD END</b><i>↩</i><b>NEXT EXIT</b>','merge':'<b>A + B</b><i>→</i><b>1 SIGNAL</b>','hinge':'<b>STATE A</b><i>↔</i><b>STATE B</b>','resonator':'<b>C I–II ×2</b><i>→</i><b>C III–V ×3</b>','forge':'<b>★ I–II ×2</b><i>→</i><b>★ III ×3</b>','foundation':'<b>MARKET 1 ×2</b><i>→</i><b>MARKET 3 ×3</b>','mirror':'<b>5</b><i>— [MR] —</i><b>5</b>','mint':'<b>FIRST SCORE</b><i>→</i><b>+1c</b>','long-chain':'<b>10+ TILES</b><i>→</i><b>ALL ★ PAY</b>'};
    return '<div class="modDiagram modDiagramLine'+cls+'" role="img" aria-label="'+escapeHtml(g.market)+'">'+(map[g.visual]||'<b>'+escapeHtml(g.code)+'</b>')+'</div>';
  }
  function pieceFor(state,tileId){return(state&&state.pieces||[]).find(p=>p&&p.tile&&p.tile.id===tileId)||null}
  function result(stateName,label,detail){return Object.freeze({state:stateName,label,detail})}
  function active(yes,label,detail){return result(yes?'active':'inactive',yes?label:'INACTIVE',detail)}
  function status(id,state,tileId){
    const p=pieceFor(state,tileId),pieces=state&&state.pieces||[],geometryReady=!!p&&Array.isArray(p.cubes)&&p.cubes.length>0&&pieces.every(q=>Array.isArray(q.cubes)&&q.cubes.length>0),facts=geometryReady&&E&&E.modGeometryFacts?E.modGeometryFacts(p,pieces):null,rank=Math.max(0,Number(state&&state.circuitRanks&&state.circuitRanks[tileId])||0),tier=Math.max(0,Number(p&&p.tile&&p.tile.upgrade)||0);
    if(id==='double-double')return result('ready','READY','Both halves on the first activation this Move.');
    if(id==='double-echo')return result('ready','READY','One Echo on the first activation this Move.');
    if(id==='zero-port'){const linked=(state&&state.zeroPortTileIds||[]).length===2&&(state.zeroPortTileIds||[]).includes(tileId);return result(linked?'active':'inactive',linked?'LINKED':'INACTIVE',linked?'Paired with the other Zero Port.':'Install a second Zero Port.')} 
    if(id==='parity-exchange')return result('active','ACTIVE','ODD adds · EVEN multiplies.');
    if(id==='corner')return active(!!(facts&&facts.corner),'ACTIVE ×3',(facts?facts.connectionCount:0)+' neighbours');
    if(id==='long-line'){const n=facts&&facts.straightLineLength||0,m=n>=5?3:n>=3?2:1;return active(n>=3,'ACTIVE ×'+m,n?n+' tiles in the physical line':'Needs 2 opposite neighbours')}
    if(id==='overload'){const n=facts&&facts.connectionCount||0;return result(n>1?'active':'ready','CURRENT ×'+Math.max(1,n),n+' physical neighbour'+(n===1?'':'s'))}
    if(id==='terminal')return active(facts&&facts.connectionCount===1,'ACTIVE ×3',(facts&&facts.connectionCount||0)+' physical neighbours');
    if(id==='triple-double')return active(!!(facts&&facts.fullCross),'READY · 3-WAY SPLIT',facts&&facts.fullCross?'Complete cross':'Needs all 4 sides connected');
    if(id==='diode'){const half=state&&Number.isInteger(state.diodeInHalf)?state.diodeInHalf:0;return result('ready','IN HALF '+(half+1),'Only this entry direction passes.');}
    if(id==='return')return result('ready','READY · RETURN','First dead branch can retry another exit once.');
    if(id==='twin')return active(!!(facts&&facts.twin),'ACTIVE ×3',facts&&facts.twin?'Identical neighbour connected':'Needs an identical printed neighbour');
    if(id==='pair')return active(!!(facts&&facts.pair),'ACTIVE ×3',facts&&facts.pair?'Exact parallel 2×2 block':'Needs one aligned parallel neighbour');
    if(id==='bridge')return active(!!(facts&&facts.bridge),'ACTIVE ×3',facts&&facts.bridge?'Removing this tile splits the machine':'Not a structural bridge');
    if(id==='gate')return active(!!(facts&&facts.gate),'ACTIVE ×2',facts&&facts.gate?'One neighbour on each half':'Needs exactly 2 neighbours, one per half');
    if(id==='fan')return active(!!(facts&&facts.fan),'ACTIVE ×4',facts&&facts.fan?'3 neighbours around one half':'Needs exactly 3 around one half');
    if(id==='frame')return active(!!(facts&&facts.frame),'ACTIVE ×2',facts&&facts.frame?'Inside a closed physical loop':'Not currently in a loop');
    if(id==='crown')return active(!!(facts&&facts.crown),'ACTIVE ×4',facts&&facts.crown?'3-sided junction spans both halves':'Needs 3 sides across both halves');
    if(id==='frontier')return active(!!(facts&&facts.frontier),'ACTIVE ×2',facts&&facts.frontier?'A full long side is still clear':'Needs 2+ neighbours and one clear long side');
    if(id==='merge')return result('ready','READY · MERGE','Needs 2 sibling signals through the same half.');
    if(id==='hinge'){const h=state&&state.hingeState;return result('ready',h?'STATE '+(h.active===1?'B':'A'):'READY · HINGE',h?'Pivot '+h.pivotTileId:'Needs a valid mirrored state.');}
    if(id==='resonator'){const mult=rank>=3?3:rank>0?2:1;return active(rank>0,'ACTIVE ×'+mult,rank?'Circuit rank '+((D&&D.CIRCUIT_RANKS&&D.CIRCUIT_RANKS[rank-1]&&D.CIRCUIT_RANKS[rank-1].roman)||rank):'No Circuit rank on this tile')}
    if(id==='forge'){const mult=tier>=3?3:tier>0?2:1;return active(tier>0,'ACTIVE ×'+mult,tier?'Star '+tier:'No Stars on this tile')}
    if(id==='foundation'){const assigned=Number.isInteger(state&&state.foundationAssignedMarket)?state.foundationAssignedMarket:(state&&state.marketCount||0),age=Math.max(0,(state&&state.marketCount||0)-assigned),mult=age>=3?3:age>=1?2:1;return active(age>=1,'ACTIVE ×'+mult,age+' Market'+(age===1?'':'s')+' survived')}
    if(id==='knot'){let count=0;if(geometryReady&&C&&C.adjacency&&C.cycleSignaturesThrough){const graph=C.adjacency(pieces,E.contactBetweenPieces);count=C.cycleSignaturesThrough(graph,tileId,(D&&D.KNOT_MIN_CYCLE_SIZE)||4).length}return active(count>=2,'ACTIVE ×4',geometryReady?count+' distinct physical loop'+(count===1?'':'s'):'Geometry unavailable in this snapshot')}
    if(id==='mirror'){const values=facts&&facts.mirrorValues;return active(!!(facts&&facts.mirror),'ACTIVE ×3',values?'Outward values '+values[0]+' and '+values[1]:'Needs one distinct neighbour on each half')}
    if(id==='mint'){const ready=!state||state.mintPaidRound!==state.round;return result(ready?'ready':'spent',ready?'READY · +1c':'SPENT THIS ROUND',ready?'Next positive scoring activation pays.':'Available again next round.')}
    if(id==='long-run')return result('machine','MACHINE MOD','Activates on a route of 10+ unique tiles.');
    return result('ready','INSTALLED','');
  }
  return Object.freeze({get,all:()=>Object.keys(G).map(get),diagramHtml,status});
});
