{
  "name": "iterion-prototype",
  "private": true,
  "scripts": {
    "test:browser": "playwright test tests/browser-smoke.spec.js --reporter=line"
  },
  "devDependencies": {
    "@playwright/test": "1.63.0"
  }
}
