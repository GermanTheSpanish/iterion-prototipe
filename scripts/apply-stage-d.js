const fs=require('fs');
function replace(path,from,to,label){let src=fs.readFileSync(path,'utf8');if(!src.includes(from))throw new Error(`${label} baseline mismatch`);src=src.replace(from,to);fs.writeFileSync(path,src)}

replace('data.js',`    MARKET_ZERO_MEMORY_COST:9,\n    LONG_RUN_UNIQUE_THRESHOLD:10,`,`    MARKET_ZERO_MEMORY_COST:9,\n    MARKET_DOUBLE_ECHO_COST:10,\n    LONG_RUN_UNIQUE_THRESHOLD:10,`,'Stage D data');
replace('mods.js',`  register({id:'zero-memory',displayName:'ZERO MEMORY',name:'ZERO MEMORY',kind:'market-tile-mod',market:true,target:'zero',marketCostKey:'MARKET_ZERO_MEMORY_COST',shortDescription:'A placed zero remembers the previous scoring operation.',rulesDescription:'On this physical zero tile’s first rebound each Move, repeat exactly once the immediately preceding non-zero scoring operation, then continue the normal rebound. Zero charges and routing are unchanged.',description:'On its first rebound each Move, repeat the previous non-zero scoring operation once.'});\n  return{register,get,all};`,`  register({id:'zero-memory',displayName:'ZERO MEMORY',name:'ZERO MEMORY',kind:'market-tile-mod',market:true,target:'zero',marketCostKey:'MARKET_ZERO_MEMORY_COST',shortDescription:'A placed zero remembers the previous scoring operation.',rulesDescription:'On this physical zero tile’s first rebound each Move, repeat exactly once the immediately preceding non-zero scoring operation, then continue the normal rebound. Zero charges and routing are unchanged.',description:'On its first rebound each Move, repeat the previous non-zero scoring operation once.'});\n  register({id:'double-echo',displayName:'DOUBLE ECHO',name:'DOUBLE ECHO',kind:'market-tile-mod',market:true,target:'double',marketCostKey:'MARKET_DOUBLE_ECHO_COST',shortDescription:'A placed double sends one echo down its chosen downstream route.',rulesDescription:'On this physical non-zero double’s first activation each Move, an Echo copies the current Output and follows the already chosen downstream route once. Final Output is Main + Echo. Echoes cannot create Echoes.',description:'Its first activation sends one non-recursive Echo down the chosen downstream route.'});\n  return{register,get,all};`,'Stage D mods');

replace('engine.js',`  function bestSignal(newPieceId,pieces,opts={}){`,`  function replaySelectedEcho(result,opts={}){\n    if(!opts.doubleEchoPieceId)return result;\n    let echoActive=false,echoOutput=0,echoRebounds=0;const events=[];\n    for(const raw of result.events||[]){\n      events.push({...raw});\n      if(!echoActive&&raw.type==='op'&&raw.piece===opts.doubleEchoPieceId){echoActive=true;echoOutput=raw.after;events.push({type:'double-echo-start',piece:raw.piece,startOutput:echoOutput});continue}\n      if(!echoActive)continue;\n      if(raw.type==='op'){const before=echoOutput,v=raw.value;if(v===2||v===4||v===6)echoOutput+=v;else if(v===1||v===3||v===5)echoOutput*=v;events.push({type:'echo-op',piece:raw.piece,value:v,op:v===0?'zero':v%2===0?'add':'multiply',before,after:echoOutput,add:v!==0&&v%2===0?v:0,factor:v%2===1?v:0,reverse:!!raw.reverse})}\n      else if(raw.type==='rebound'){echoRebounds++;events.push({type:'echo-rebound',piece:raw.piece,charge:raw.charge})}\n    }\n    if(!echoActive)return result;\n    const mainOutput=result.output,output=mainOutput+echoOutput;\n    const die=events.length&&events[events.length-1].type==='die'?events.pop():null;events.push({type:'double-echo-result',piece:opts.doubleEchoPieceId,mainOutput,echoOutput,finalOutput:output,echoRebounds});if(die)events.push(die);\n    return{...result,mainOutput,echoOutput,output,gain:output-(opts.initialOutput||0),events,doubleEchoActivated:true,echoRebounds}\n  }\n  function bestSignal(newPieceId,pieces,opts={}){`,'Stage D echo helper');
replace('engine.js',`    best=best||{output:initialOutput,gain:0,path:[],segments:[],events:[],reason:'no-route',traversals:0,rebounds:0};best.search={starts:starts.length,expanded,leaves,truncated};return replaySelectedScoring(best,initialOutput,opts)\n  }`,`    best=best||{output:initialOutput,gain:0,path:[],segments:[],events:[],reason:'no-route',traversals:0,rebounds:0};best.search={starts:starts.length,expanded,leaves,truncated};best=replaySelectedScoring(best,initialOutput,opts);return replaySelectedEcho(best,{...opts,initialOutput})\n  }`,'Stage D post-route echo');
replace('engine.js',`applyOp,replaySelectedScoring,bestSignal,simulateSignal`,`applyOp,replaySelectedScoring,replaySelectedEcho,bestSignal,simulateSignal`,'Stage D engine export');

replace('game.js',`doubleDoubleTileId:null,zeroMemoryTileId:null};`,`doubleDoubleTileId:null,zeroMemoryTileId:null,doubleEchoTileId:null};`,'Stage D state');
replace('game.js',`    const doubleDoublePieceId=s.pieces.find(x=>x.tile.id===s.doubleDoubleTileId)?.id||null,zeroMemoryPieceId=s.pieces.find(x=>x.tile.id===s.zeroMemoryTileId)?.id||null;\n    const sim=s.pieces.length===1?{output:trigger,events:[],reason:'root',rebounds:0,search:{starts:0,leaves:1,expanded:0}}:E.bestSignal(p.id,s.pieces,{initialOutput:trigger,doubleDoublePieceId,zeroMemoryPieceId});`,`    const doubleDoublePieceId=s.pieces.find(x=>x.tile.id===s.doubleDoubleTileId)?.id||null,zeroMemoryPieceId=s.pieces.find(x=>x.tile.id===s.zeroMemoryTileId)?.id||null,doubleEchoPieceId=s.pieces.find(x=>x.tile.id===s.doubleEchoTileId)?.id||null;\n    const sim=s.pieces.length===1?{output:trigger,events:[],reason:'root',rebounds:0,search:{starts:0,leaves:1,expanded:0}}:E.bestSignal(p.id,s.pieces,{initialOutput:trigger,doubleDoublePieceId,zeroMemoryPieceId,doubleEchoPieceId});`,'Stage D game opts');
replace('game.js',`    if(m.target==='double')return placed.filter(t=>isDouble(t)&&t.a>0&&(id!=='double-double'||t.id!==s.doubleDoubleTileId));`,`    if(m.target==='double')return placed.filter(t=>isDouble(t)&&t.a>0&&(id!=='double-double'||t.id!==s.doubleDoubleTileId)&&(id!=='double-echo'||t.id!==s.doubleEchoTileId));`,'Stage D double targets');
replace('game.js',`previousTileId=id==='double-double'?(s.doubleDoubleTileId||null):id==='zero-memory'?(s.zeroMemoryTileId||null):null}\n    if(id!=='double-double'&&id!=='long-run'&&id!=='zero-memory')return{ok:false,reason:'unsupported'};`,`previousTileId=id==='double-double'?(s.doubleDoubleTileId||null):id==='double-echo'?(s.doubleEchoTileId||null):id==='zero-memory'?(s.zeroMemoryTileId||null):null}\n    if(id!=='double-double'&&id!=='double-echo'&&id!=='long-run'&&id!=='zero-memory')return{ok:false,reason:'unsupported'};`,'Stage D buy support');
replace('game.js',`    if(id==='double-double')s.doubleDoubleTileId=tile.id;\n    if(id==='zero-memory')s.zeroMemoryTileId=tile.id;`,`    if(id==='double-double')s.doubleDoubleTileId=tile.id;\n    if(id==='double-echo')s.doubleEchoTileId=tile.id;\n    if(id==='zero-memory')s.zeroMemoryTileId=tile.id;`,'Stage D assign target');

const engineTest=`const assert=require('assert');const fs=require('fs');const E=require('../engine.js');\nE.setBoardSize(18,24);const entry=E.pieceFrom({a:1,b:5},0,0,0,0,3);entry.tile={id:'entry',a:1,b:5};const dbl=E.pieceFrom({a:5,b:5},4,0,0,0,1);dbl.tile={id:'echo',a:5,b:5};const zero=E.pieceFrom({a:5,b:0},8,0,0,0,2);zero.tile={id:'zero',a:5,b:0};const pieces=[entry,dbl,zero];\nconst base=E.bestSignal(entry.id,pieces,{initialOutput:6}),echo=E.bestSignal(entry.id,pieces,{initialOutput:6,doubleEchoPieceId:dbl.id});\nassert.strictEqual(base.output,750);assert.strictEqual(echo.mainOutput,750);assert.strictEqual(echo.echoOutput,750);assert.strictEqual(echo.output,1500,'Final Output must equal Main + Echo');\nassert.strictEqual(echo.events.filter(e=>e.type==='double-echo-start').length,1,'exactly one Echo starts per Move');assert.strictEqual(echo.events.filter(e=>e.type==='double-echo-result').length,1);\nassert.strictEqual(echo.events.filter(e=>e.type==='double-echo-start'&&e.piece===dbl.id).length,1,'Echo cannot recursively create another Echo');\nassert.strictEqual(echo.echoRebounds,1,'Echo follows the same downstream rebound path');assert.deepStrictEqual(echo.path,base.path,'Echo must not alter selected main path');assert.deepStrictEqual(echo.segments,base.segments);\nconst both=E.bestSignal(entry.id,pieces,{initialOutput:6,doubleEchoPieceId:dbl.id,zeroMemoryPieceId:zero.id});assert.strictEqual(both.mainOutput,3750);assert.strictEqual(both.echoOutput,750);assert.strictEqual(both.output,4500);assert.strictEqual(both.events.filter(e=>e.type==='zero-memory').length,1);\nconst source=fs.readFileSync(require.resolve('../engine.js'),'utf8');assert(source.includes('const av=[a.traversals||0,a.output||0,a.rebounds||0,(a.path||[]).length]'));\nconsole.log('v0.23 Stage D Double Echo isolated engine tests passed');\n`;
fs.writeFileSync('tests/v023-stage-d-engine.test.js',engineTest);
const gameTest=`const assert=require('assert');const D=require('../data.js');const E=require('../engine.js');const Game=require('../game.js');\nassert.strictEqual(D.VERSION,'0.22.1');assert.strictEqual(D.MARKET_DOUBLE_ECHO_COST,10);\nE.setBoardSize(30,40);const game=Game.createGame(E,{seed:2304,STARTING_COINS:100,TARGETS:Array(15).fill(1e15)}),s=game.state();const doubles=s.set.filter(t=>t.a===t.b&&t.a>0).slice(0,2);s.pieces=doubles.map((t,i)=>{const p=E.pieceFrom(t,i*6,0,0,0,100+i);p.tile={...t};return p});s.placedTileIds=doubles.map(t=>t.id);s.cleared=true;s.round=2;s.nextShopType='market';s.intermissionResolved=false;s.coins=100;assert(game.openIntermission());assert(s.shopOffers.includes('double-echo'));const buy=game.buyMarketMod('double-echo');assert(buy.ok);assert.strictEqual(s.doubleEchoTileId,buy.tile.id);assert.strictEqual(buy.cost,10);\nconst long=Game.createGame(E,{seed:9,TARGETS:Array(15).fill(1e15)}),ls=long.state();ls.mods=['long-run'];ls.pieces=[];for(let i=1;i<=10;i++){const tile={id:'de-star-'+i,a:2,b:2,upgrade:i<=3?[3,2,1][i-1]:0,source:'test'},p=E.pieceFrom(tile,(i%5)*4,Math.floor(i/5)*4,0,0,i);p.tile=tile;ls.pieces.push(p)}ls.running=true;ls.roundTurn=1;ls.turn=1;const events=[];for(const p of ls.pieces)events.push({type:'op',piece:p.id,value:2,op:'add',add:2,before:0,after:2});for(const p of ls.pieces.slice(2))events.push({type:'echo-op',piece:p.id,value:2,op:'add',add:2,before:0,after:2});const r=long.finishPlacement({ok:true,tile:{id:'ctx',a:1,b:2},p:ls.pieces[0],trigger:3,sim:{output:100,mainOutput:60,echoOutput:40,events,reason:'test',rebounds:0,search:{starts:1,leaves:1,expanded:1}}});assert.strictEqual(r.upgradeCoins,6,'Echo must not pay physical stars a second time under Long Run');\nconsole.log('v0.23 Stage D Double Echo Market / Long Run interaction tests passed');\n`;
fs.writeFileSync('tests/v023-stage-d-game.test.js',gameTest);fs.unlinkSync(__filename);
